// komponent (plik) App.js z folderu components. W metodzie .render() tworzymy listę kontaktów na podstawie przekazanych parametrów.

var app = React.createElement(App);
ReactDOM.render(app, document.getElementById('app'));